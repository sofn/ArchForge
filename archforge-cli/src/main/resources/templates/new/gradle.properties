org.gradle.jvmargs=-Xmx2g
org.gradle.parallel=true
org.gradle.caching=true

# ArchForge release-set BOM version — the single version knob for all
# com.lesofn.archforge:* dependencies (see build.gradle.kts platforms).
archforgeVersion=%ARCHFORGE_VERSION%
