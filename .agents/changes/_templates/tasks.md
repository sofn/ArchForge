# Tasks: <topic>

Order: data model → API contract → low-level impl → orchestration → entry points.

| # | Task | Files (exact paths + signatures) | Verify | Status |
|---|------|----------------------------------|--------|--------|
| T1 | <name> | `a/b/C.java` — `C.foo(int)` | `./gradlew :m:compileJava :m:checkstyleMain` | pending |
| T2 | <name> | | | pending |

Status values: `pending` / `in_progress` / `done` / `blocked(<reason>)`
