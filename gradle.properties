# JVM arguments for Gradle daemon
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m

# Enable parallel builds
org.gradle.parallel=true

# Enable build caching
org.gradle.caching=true

# Disable configuration caching due to GraalVM native plugin serialization issue
org.gradle.configuration-cache=false

# Disable isolated projects (Gradle 8.5+ feature)
org.gradle.configuration-cache.parallel=false

# Single-source project version — consumed by root build.gradle.kts and archforge-dependencies BOM
archforgeVersion=0.1.0-SNAPSHOT
