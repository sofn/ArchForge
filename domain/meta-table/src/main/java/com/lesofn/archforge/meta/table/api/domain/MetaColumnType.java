package com.lesofn.archforge.meta.table.api.domain;

/**
 * 元表格字段数据类型。
 */
public enum MetaColumnType {
    STRING,
    TEXT,
    INTEGER,
    DECIMAL,
    BOOLEAN,
    DATE,
    DATETIME,
    ENUM,
    JSON,
    FILE,
    UUID,
    TIMESTAMPTZ,
    ARRAY,
    GEO
}
