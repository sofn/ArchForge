# Propagate jspecify @Nullable from fields onto Lombok-generated
# getters (return type) and setters (parameter).
config.stopBubbling = true
lombok.copyableAnnotations += org.jspecify.annotations.Nullable
